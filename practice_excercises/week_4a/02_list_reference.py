# List reference problem

###################################################
# Student should enter code below

a = [5, 3, 1, -1, -3, 5]
b = a

b[0] = 0
print 'First entry of a: [%s]' % a[0]

###################################################
# Explanation

# Arrays are single location on the memory
# a Single copy exists, hence making an "copy" of the array
# only makes a new pointer to the same data
